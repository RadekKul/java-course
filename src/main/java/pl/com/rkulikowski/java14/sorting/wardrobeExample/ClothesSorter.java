package pl.com.rkulikowski.java14.sorting.wardrobeExample;

public class ClothesSorter {

    // wykonac metody sortujace po rozmiarach w dol i w gore, po nazwach i po czystosci, popropowac thenCompare
    // sprobowac wrzucic ogolna metode np int z parametrami dwoma obiektow niestatyczna i nadpisywac nia nowy sorter w mainie za kazdym razem za pomoca lambdy np ClothesSorter s = x->x.getcos.compareTo(getcos)
}
